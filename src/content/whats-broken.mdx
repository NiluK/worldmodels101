Every chapter so far has ended with something that does not work yet.

This one collects them, because the gap between what these systems can do and
what the announcements imply is where most of the confusion in this subject
lives, and because knowing what is broken is the difference between reading a
paper and being sold one.

## Rollouts do not fail all at once

An average error over a rollout collapses distinct failures into one number.
Identity can fail while texture remains sharp; action fidelity can fail while
both look fine. The order depends on the system and task, so the useful report
is not one horizon but a set of property-specific horizons.

<Figure
  n="9.1"
  caption={<>A diagnostic thought experiment, not a benchmark result. Its ordering is one plausible failure profile chosen to expose what an average hides. A different model may lose physics before identity or preserve layout longer. Drag the horizon. "Still looks good" is not the same claim as "still supports the task."</>}
>
  <WhatBreaks />
</Figure>

In this constructed profile, colour and texture outlast identity and physical
behaviour. That ordering is not universal. Per-frame losses directly reward
surfaces and light, while persistence has to be inferred across
time.

Which is precisely backwards from what you want, and it is not a coincidence.
A loss computed per frame rewards getting most of the pixels approximately
right, and most of the pixels are surfaces and light. Nothing in that loss is
watching whether the chair you walked past is the same chair.

So a rollout can be a hundred steps past the point of being useful and still
look, in any single frame, completely fine. That is the specific way this
subject is easy to fool yourself about.

## The three that keep coming back

**Things stop being themselves.** Object permanence is the one everyone
notices. In a renderer with no exposed object store, identity is carried by the
predictor's hidden state rather than enforced by a persistent record. It can
degrade gradually and without an error message when the chair becomes another
chair. Structured simulators may store the object explicitly; they pay for that
advantage by requiring the structure to be defined or learned correctly.

**What if is not really available.** A model can tell you what is likely to
happen next. Asking what would have happened if you had done something else is
a different question, and the answer you get is true inside the model rather
than about the world. Where the model has data the two are close. Where it does
not, they are not, and nothing in the answer indicates which case you are in.

<MarginNote>
This one is easy to under-rate because the model always answers. A system that
said "I have never seen anything like that" would be more useful and less
impressive, and nobody is rewarded for building it.
</MarginNote>

**Long horizons are still the binding constraint.** Every trick in this course
is, read uncharitably, a way of not needing the model to be right for very
long. Short rollouts. Replanning. Starting from real states. They work. They
also amount to an admission that the thing does not hold up, and the horizon
over which it does is the number that decides what you can build.

## There is no agreed overall winner

This is the part that should bother you most, and it gets the least attention.

Benchmarks now test generated worlds directly. PlayWorld, released in August
2026, evaluates 171 scenarios across geometry,
interaction fidelity and what happens to objects as they leave and re-enter
view. Other suites emphasise perceptual video quality. What is still missing is
an accepted way to turn these different contracts into one overall ordering.

<Figure
  n="9.2"
  caption={<>The scores are invented and the systems are not real. The disagreement is real. There is no accepted way to combine these measures into one ordering. Click through the four and watch the winner change.</>}
>
  <NoRanking />
</Figure>

Frame quality is measurable and is what demos are made of. Whether the world
holds together over a thousand steps is what an agent needs, and no demo is
long enough to show it. Whether the action you gave it is the action it took
is the difference between a world model and a video, and it is almost never
reported. Cost decides whether anything can afford to search inside the thing,
and it is reported least of all.

Downstream task success looks like the honest answer, and it has its own
problem. It scores the model and the policy together, so a good policy hides a
bad model and the number tells you nothing about which you have.

Progress is real. "Better" is incomplete until the measure and system contract
are named.

## Match the failure test to the system

The phrase covers five different machines, and they should not face the same
exam. A renderer lives or dies by persistence and action fidelity. A simulator
is the easy one to examine, because it exports structure another program can
check. A compact dynamics model lives or dies by rollout horizon and whether
planning search can exploit it. A representation can look stable while
discarding the variable the task needed. An implicit model needs a probe
capable of falsifying the claim that the model is there at all.

<Figure
  n="9.3"
  caption={<>Five system contracts crossed with five failure questions. The levels are editorial judgements, not benchmark scores. Select a row to reveal the question that most often binds that kind of system. A single "world model quality" axis would hide all five.</>}
>
  <FailureMatrix />
</Figure>

Four questions still handle most papers:

**What does it output?** A picture, a structure, a compact state, an embedding.
This decides what it can be used for and almost nothing else follows without
it.

**Can you roll it forward under actions nobody took?** If not, it is not going
to support a decision, however good it looks.

**Over what horizon does it hold up, and who measured that?** The number
matters more than the architecture, and it is usually missing.

**Which measure is the claim using, and what does that measure ignore?** There
is no overall score. Anyone implying there is has picked one and not told you.

Those questions force a paper to name its output, its evidence and the horizon
over which its claim holds. Architecture names alone do none of that work.

## Try it

<Figure
  n="9.4"
  caption={<>Eight questions on the honest state of it. If you have read the whole course, the last one should be easy and slightly annoying.</>}
>
  <Quiz chapter={9} />
</Figure>

---

That is the course.

Nine chapters ago the phrase meant five different things and nobody said which.
It still does. What has changed is that you can now tell which one is on the
table, and what it would take for the claim to be true. And what to ask when
somebody is not saying.

Thanks for reading. If something here is wrong, and some of it will be, the
correction is genuinely welcome and will be credited.

## Sources

<Figure
  n="9.5"
  caption={<>Ordered for someone building on this rather than for historical completeness.</>}
>
  <SourceList chapter={9} />
</Figure>
